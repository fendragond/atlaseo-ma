---
interface Props {
	href: string;
	label: string;
	variant?: 'icon-only' | 'with-label';
}

const { href, label, variant = 'icon-only' } = Astro.props;

const linkClass =
	variant === 'icon-only'
		? 'social-link-hover inline-flex min-h-11 min-w-11 items-center justify-center rounded'
		: 'social-link-hover inline-flex min-h-11 items-center gap-2 text-sm';
---

<a href={href} target="_blank" rel="noopener noreferrer" class={linkClass} aria-label={label}>
	<slot />
	{variant === 'with-label' && <span>{label}</span>}
</a>
